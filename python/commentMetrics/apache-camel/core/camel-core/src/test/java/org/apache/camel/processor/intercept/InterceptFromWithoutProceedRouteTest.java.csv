commented;modifiers;parameterAmount;loc;comment;code
false;public;0;5;;public void configure() {     interceptFrom().filter(header("foo").isEqualTo("bar")).to("mock:b").     from("direct:start").to("mock:a"). }
false;protected;0;10;;@Override protected RouteBuilder createRouteBuilder() {     return new RouteBuilder() {          public void configure() {             interceptFrom().filter(header("foo").isEqualTo("bar")).to("mock:b").             from("direct:start").to("mock:a").         }     }. }
false;protected;0;5;;@Override protected void prepareMatchingTest() {     a.expectedMessageCount(1).     b.expectedMessageCount(1). }
false;protected;0;5;;@Override protected void prepareNonMatchingTest() {     a.expectedMessageCount(1).     b.expectedMessageCount(0). }

commented;modifiers;parameterAmount;loc;comment;code
false;public;0;4;;public void configure() {     onException(Exception.class).handled(true).to("mock:exception").     from("direct:start").routingSlip(header(ROUTING_SLIP_HEADER)).to("mock:noexception"). }
false;protected;0;8;;protected RouteBuilder createRouteBuilder() {     return new RouteBuilder() {          public void configure() {             onException(Exception.class).handled(true).to("mock:exception").             from("direct:start").routingSlip(header(ROUTING_SLIP_HEADER)).to("mock:noexception").         }     }. }

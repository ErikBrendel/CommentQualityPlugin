commented;modifiers;parameterAmount;loc;comment;code
false;public;0;8;;@Override public void configure() throws Exception {     // generate random number every second     // which is send to this seda queue that the NumberPojo will consume     from("timer:number?period=1s").transform().simple("${random(0,200)}").to("direct:numbers"). }
